<!DOCTYPE html>
<html>
  <head>
    <title><%= item.name %></title>
    <style>
      body {
        font-family: Arial, sans-serif;
        margin: 0;
        padding: 0;
      }
      .container {
        max-width: 800px;
        margin: 0 auto;
        padding: 20px;
      }
      h1 {
        font-size: 36px;
        margin-bottom: 10px;
      }
      p {
        font-size: 18px;
        line-height: 1.5;
        margin-bottom: 20px;
      }
      .date {
        font-size: 14px;
        color: #999;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <h1><%= item.name %></h1>
      <p><%= item.description %></p>
      <p class="date">Date: <%= item.date.toDateString() %></p>
    </div>
  </body>
</html>
